_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
.DS_Store
node_modules/
.env

# Written by scripts/ on every deploy — never committed, never stale.
_data/live/
assets/shots/

# Held back until DNS for links.imswarnil.com exists. Rename to CNAME and
# commit once the record resolves — see README "Domain".
CNAME.pending
